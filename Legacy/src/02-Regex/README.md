# Regex

这一章讨论正则表达式。

放在这本书里，正则的意义不只是“写几个模式去匹配字符串”，而是它第一次让文本处理显露出一种重要分野：规则系统可以非常强大，但也有明确边界。

因此本章会关心三件事：

- 正则表达式在形式语言里到底是什么
- 一个 matcher 最小可以怎么实现
- 为什么它适合做局部模式匹配，却不适合承载更复杂的结构理解

这一章往前接编码，往后接 Trie 和分词，是因为它让我们第一次直面“文本规则”和“文本结构”之间的差异。
