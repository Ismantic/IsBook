# Trie

这一章围绕 Trie 及其变种展开。

如果说正则表达式更像是在描述模式，那么 Trie 关心的则是另一类问题：当你手里有一个词典，怎样高效地做前缀查询、最长匹配和候选枚举。

这件事在 NLP 里非常核心，因为：

- 分词要依赖词典前缀搜索
- tokenizer 训练和解码会频繁访问词表
- 很多文本工具本质上都在做“从当前位置出发，看看能匹配到哪些词”

本章会从普通 Trie 出发，继续进入 Critbit Trie 和 Double-Array Trie，看看相同的问题如何被不同的数据结构设计处理。
