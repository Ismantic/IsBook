# Unicode

这一章从字符编码讲起。

对 NLP 来说，这不是一个可以跳过的“准备知识”，而是整条技术链的起点。因为后面几乎所有问题都依赖同一件事：你到底怎样表示文本。

很多实现中的混乱，最后都能追溯到最初这里没有分清楚几层概念：

- 码点是什么
- 字符串是什么
- 字节序列是什么
- UTF-8 为什么既节省空间，又能保持兼容性

这一章的任务，就是先把这几个基本层次理清。只有把文本当作“有结构的字节序列”来理解，后面的正则、Trie、分词和 tokenizer 才会真正变得自然。
